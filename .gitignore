# Operator-only: scripts, hooks, deployment notes, agent working files.
# Not published on GitHub.
.local-use-only/

# Deep full-volume exports are local analysis products only.
# They are intentionally NOT part of the GitHub-ready build/ tree.
build/deep/
__pycache__/
